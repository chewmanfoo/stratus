require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe CheckoutResultsController do

  # This should return the minimal set of attributes required to create a valid
  # CheckoutResult. As you add validations to CheckoutResult, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    { "name" => "MyString" }
  end

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # CheckoutResultsController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET #index" do
    it "assigns all checkout_results as @checkout_results" do
      checkout_result = CheckoutResult.create! valid_attributes
      get :index, {}, valid_session
      assigns(:checkout_results).should eq([checkout_result])
    end
  end

  describe "GET show" do
    it "assigns the requested checkout_result as @checkout_result" do
      checkout_result = CheckoutResult.create! valid_attributes
      get :show, {:id => checkout_result.to_param}, valid_session
      assigns(:checkout_result).should eq(checkout_result)
    end
  end

  describe "GET new" do
    it "assigns a new checkout_result as @checkout_result" do
      get :new, {}, valid_session
      assigns(:checkout_result).should be_a_new(CheckoutResult)
    end
  end

  describe "GET edit" do
    it "assigns the requested checkout_result as @checkout_result" do
      checkout_result = CheckoutResult.create! valid_attributes
      get :edit, {:id => checkout_result.to_param}, valid_session
      assigns(:checkout_result).should eq(checkout_result)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new CheckoutResult" do
        expect {
          post :create, {:checkout_result => valid_attributes}, valid_session
        }.to change(CheckoutResult, :count).by(1)
      end

      it "assigns a newly created checkout_result as @checkout_result" do
        post :create, {:checkout_result => valid_attributes}, valid_session
        assigns(:checkout_result).should be_a(CheckoutResult)
        assigns(:checkout_result).should be_persisted
      end

      it "redirects to the created checkout_result" do
        post :create, {:checkout_result => valid_attributes}, valid_session
        response.should redirect_to(CheckoutResult.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved checkout_result as @checkout_result" do
        # Trigger the behavior that occurs when invalid params are submitted
        CheckoutResult.any_instance.stub(:save).and_return(false)
        post :create, {:checkout_result => { "name" => "invalid value" }}, valid_session
        assigns(:checkout_result).should be_a_new(CheckoutResult)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        CheckoutResult.any_instance.stub(:save).and_return(false)
        post :create, {:checkout_result => { "name" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested checkout_result" do
        checkout_result = CheckoutResult.create! valid_attributes
        # Assuming there are no other checkout_results in the database, this
        # specifies that the CheckoutResult created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        CheckoutResult.any_instance.should_receive(:update_attributes).with({ "name" => "MyString" })
        put :update, {:id => checkout_result.to_param, :checkout_result => { "name" => "MyString" }}, valid_session
      end

      it "assigns the requested checkout_result as @checkout_result" do
        checkout_result = CheckoutResult.create! valid_attributes
        put :update, {:id => checkout_result.to_param, :checkout_result => valid_attributes}, valid_session
        assigns(:checkout_result).should eq(checkout_result)
      end

      it "redirects to the checkout_result" do
        checkout_result = CheckoutResult.create! valid_attributes
        put :update, {:id => checkout_result.to_param, :checkout_result => valid_attributes}, valid_session
        response.should redirect_to(checkout_result)
      end
    end

    describe "with invalid params" do
      it "assigns the checkout_result as @checkout_result" do
        checkout_result = CheckoutResult.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        CheckoutResult.any_instance.stub(:save).and_return(false)
        put :update, {:id => checkout_result.to_param, :checkout_result => { "name" => "invalid value" }}, valid_session
        assigns(:checkout_result).should eq(checkout_result)
      end

      it "re-renders the 'edit' template" do
        checkout_result = CheckoutResult.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        CheckoutResult.any_instance.stub(:save).and_return(false)
        put :update, {:id => checkout_result.to_param, :checkout_result => { "name" => "" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested checkout_result" do
      checkout_result = CheckoutResult.create! valid_attributes
      expect {
        delete :destroy, {:id => checkout_result.to_param}, valid_session
      }.to change(CheckoutResult, :count).by(-1)
    end

    it "redirects to the checkout_results list" do
      checkout_result = CheckoutResult.create! valid_attributes
      delete :destroy, {:id => checkout_result.to_param}, valid_session
      response.should redirect_to(checkout_results_url)
    end
  end

end
