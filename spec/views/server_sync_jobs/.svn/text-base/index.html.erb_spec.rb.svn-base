require 'spec_helper'

describe "server_sync_jobs/index" do
  before(:each) do
    assign(:server_sync_jobs, [
      stub_model(ServerSyncJob),
      stub_model(ServerSyncJob)
    ])
  end

  it "renders a list of server_sync_jobs" do
    render
    # Run the generator again with the --webrat flag if you want to use webrat matchers
  end
end
