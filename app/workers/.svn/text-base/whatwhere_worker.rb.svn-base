class WhatwhereWorker < GenericWorker

  def do_perform()
#      @task = "#{@ssh_cmd} check_switch.pl -switch #{@event.name} -host #{@server}"

#      raise UnixFailedExitException unless perform_unix("CheckSwitchWorker", @event, @task)

  end

end
