<%- model_class = ServerSyncJob -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<% unless @server_sync_job.completed? %>
  <p>
    The sync job is currently running.  The state of the job is shown below.  When the job completes, a list of
    exceptions generated by the job (things that went wrong) will display as well. Good luck.
  </p>
<% else %>
  <p>
    The sync job is complete! <%= link_to "Rerun it?", new_server_sync_job_url %>
  </p>
<% end %>

<p>
  <%= sync_job_state_bar(@server_sync_job) %>
</p>

<% unless @server_sync_job.completed? && @server_sync_job.exceptions.blank? %>
<p>
  <%= @server_sync_job.exceptions %>
</p>
<% end %>
